// Formato normal
function square(noun) {
    return num * num;
}

// Formato flecha
const square = (num) => {
    return num * num;
}

// Formato flecha con función implícita
const square = num => num * num;

